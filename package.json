{
  "scripts": {
    "ci": "lerna bootstrap && lerna run build --scope=zero-config-components && lerna run build --scope=zero-config-webpack && lerna run build --scope=zero-config-app"
  },
  "devDependencies": {
    "@types/node": "^10.12.12",
    "lerna": "^3.4.0"
  }
}
