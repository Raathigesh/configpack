import React from "react";
import { Frame, Section, Checkbox } from "zero-config-components";

export default function TypescriptForWebpack() {
  return (
    <Frame direction="column">
      <Section name="Fonts" description="Add support for fonts">
        <div />
      </Section>
    </Frame>
  );
}
