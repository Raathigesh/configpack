import Block from "./block";

const BabelForWebpack = {
  name: "Babel for Webpack",
  description: "Adds babel-loader to webpack configuration",
  component: Block
};

export default BabelForWebpack;
