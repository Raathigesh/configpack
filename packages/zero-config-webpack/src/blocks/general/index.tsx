import Block from "./block";

const GeneralWebpackConfig = {
  name: "Basic webpack configuration",
  description: "The initial webpack configuration",
  component: Block
};

export default GeneralWebpackConfig;
